//A utility library that provides a wide range of functions for working with arrays, objects, and strings. Example:

const _ = require('lodash');

const array = [1, 2, 3, 4, 5];
const sum = _.sum(array);
console.log(sum); // Output: 15
